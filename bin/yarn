#!/usr/bin/env bash
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
if [ -t 0 ]; then
    docker compose -f "$SCRIPT_DIR/docker-compose.yml" exec php yarn "$@"
else
    docker compose -f "$SCRIPT_DIR/docker-compose.yml" exec -T php yarn "$@"
fi
